<UserControl x:Class="Mordorings.UserControls.MapMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Mordorings.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewModels:MapMenuViewModel}">
    <DockPanel LastChildFill="False">
        <Button Content="Edit Map" DockPanel.Dock="Top"
                Command="{Binding Path=OpenMapEditorCommand}" />
        <Button Content="Edit Dungeon State" DockPanel.Dock="Top"
                Command="{Binding Path=OpenDungeonStateEditorCommand}" />
        <Button Content="Reveal AutoMap" DockPanel.Dock="Top" IsEnabled="False" />
        <Button Content="Monster Heatmap" DockPanel.Dock="Top"
                Command="{Binding Path=OpenMonsterHeatMapCommand}" />
    </DockPanel>
</UserControl>
